# Trig Design System

```bash
npm install git+https://github.com/PatrickMullenPavlov/design-system.git @headlessui/react @heroicons/react
```

That's it. Tailwind, styles, and CLAUDE.md are configured automatically.

```jsx
import { PrimaryButton, Card, SectionHeader } from "@trig/design-system";
```
